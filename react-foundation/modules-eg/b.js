import bleach, {x, y} from './a.js'
x();
y();
bleach() 

// in default export, we can call function with any name, for eg: here we are calling with bleach(), but we can call with other name also 
// import a, {x,y} from "./a.js"  a(); it still points to bleach function
